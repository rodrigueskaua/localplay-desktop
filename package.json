{
  "name": "localplay-desktop",
  "version": "1.0.0",
  "description": "Player local de cursos em vídeo. 100% offline.",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "install:all": "cd electron && npm install && cd ../frontend && npm install && cd ../backend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "cd electron && npm start",
    "dev": "npm-run-all --parallel dev:frontend dev:electron"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
